# PacificDB query intelligence

The Community edition includes a deterministic, read-only natural-language
query compiler and basic query/pipeline helpers. These modules use no external
AI service and reject ambiguous or write-shaped natural-language input.
